let array = ['я','умею','писать','рекурсивные','функции'];
let n = 0;
export function consoleRec(array,length){

    console.log(array[n]);
    n = n+1;
    if(n <= array.length){
        consoleRec(array,n)
    }
}
consoleRec(array,n);